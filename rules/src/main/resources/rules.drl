package com.example.rules

  global Output output;

declare Measure
    @role( event )
    @expires(5m)
end

rule "Greater Value"
when
  $t : Measure()
    t1 : Measure((value + $t.value ) > 35 , this after[0s,5m] $t)
then
  output.emit("Greater Value" );
end

